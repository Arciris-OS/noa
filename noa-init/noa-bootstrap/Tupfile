include_rules

CFLAGS += -Iinclude -std=c++20

: foreach src/*.cpp |> !cc |>
: .build/*.o |> !link |> .build/noa-bootstrap
: .build/noa-bootstrap |> ^ BIN %f^ cp %f ../../bin |> ../../bin/noa-bootstrap

