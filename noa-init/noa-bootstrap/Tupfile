include_rules
CC = gcc
CFLAGS += -Iinclude -std=c99
LINKFLAGS += -static -O2

: foreach src/*.c |> !cc |> .build/%B.o
: .build/*.o |> !link |> .build/noa-bootstrap
: .build/noa-bootstrap |> ^ CP %f -> %o^ cp %f ../../bin |> ../../bin/noa-bootstrap

